<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Catosa App - Prototipo</title>
<style>
  * { margin:0; padding:0; box-sizing:border-box; font-family:'Segoe UI', Arial, sans-serif; }
  body {
    background:#e7e9ec;
    min-height:100vh;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:24px;
  }
  .phone {
    width:390px;
    height:844px;
    background:#f5f6f8;
    border-radius:40px;
    border:10px solid #1a1a1a;
    box-shadow:0 20px 50px rgba(0,0,0,0.25);
    overflow:hidden;
    position:relative;
    display:flex;
    flex-direction:column;
  }
  .statusbar {
    height:28px;
    background:#111111;
    flex-shrink:0;
  }
  .appheader {
    background:#111111;
    color:#fff;
    padding:14px 18px 18px;
    flex-shrink:0;
  }
  .appheader-top {
    display:flex;
    justify-content:space-between;
    align-items:center;
  }
  .logo-group {
    display:flex;
    align-items:center;
    gap:10px;
  }
  .logo-group img {
    width:auto;
    display:block;
  }
  .logo-group img.logo-catosa { height:20px; }
  .logo-group img.logo-international { height:12px; }
  .logo-divider {
    width:1px;
    height:18px;
    background:rgba(255,255,255,0.25);
  }
  .profile-chip {
    display:flex;
    align-items:center;
    gap:8px;
    background:rgba(255,255,255,0.12);
    padding:6px 10px;
    border-radius:20px;
  }
  .avatar-sm {
    width:26px; height:26px; border-radius:50%;
    background:#f2a71b; color:#111111; font-weight:700;
    display:flex; align-items:center; justify-content:center;
    font-size:12px;
  }
  .points-chip { font-size:12px; font-weight:600; }
  .greeting { margin-top:14px; font-size:15px; color:#cfd9ea; }
  .greeting b { color:#fff; font-size:17px; display:block; margin-top:2px; }
  .streak {
    margin-top:12px;
    display:flex;
    align-items:center;
    gap:6px;
    background:rgba(242,167,27,0.18);
    color:#f2a71b;
    padding:6px 12px;
    border-radius:20px;
    font-size:12.5px;
    font-weight:600;
    width:fit-content;
  }
  .screen {
    flex:1;
    overflow-y:auto;
    padding:16px 16px 90px;
    display:none;
  }
  .screen.active { display:block; }
  .card {
    background:#fff;
    border-radius:16px;
    padding:16px;
    margin-bottom:14px;
    box-shadow:0 2px 6px rgba(0,0,0,0.05);
  }
  .card-title {
    font-size:14px;
    font-weight:700;
    color:#111111;
    margin-bottom:10px;
    display:flex;
    align-items:center;
    gap:6px;
  }
  .challenge-card {
    background:linear-gradient(135deg,#111111,#1a1a1a);
    color:#fff;
    border-radius:16px;
    padding:18px;
    margin-bottom:14px;
  }
  .challenge-card .tag {
    font-size:11px;
    font-weight:700;
    color:#f2a71b;
    text-transform:uppercase;
    letter-spacing:0.5px;
  }
  .challenge-card h3 { font-size:17px; margin:6px 0 4px; }
  .challenge-card p { font-size:13px; color:#cfd9ea; margin-bottom:14px; }
  .btn-primary {
    background:#f2a71b;
    color:#111111;
    border:none;
    font-weight:700;
    font-size:14px;
    padding:12px 18px;
    border-radius:12px;
    width:100%;
    cursor:pointer;
  }
  .rank-row {
    display:flex;
    align-items:center;
    gap:10px;
    padding:8px 0;
    font-size:13.5px;
    color:#2b2b2b;
    border-bottom:1px solid #f0f0f0;
  }
  .rank-row:last-child { border-bottom:none; }
  .rank-num {
    width:22px; height:22px; border-radius:50%;
    background:#eef1f5; color:#111111; font-weight:700;
    font-size:11.5px; display:flex; align-items:center; justify-content:center;
  }
  .rank-row.me { font-weight:700; color:#111111; }
  .rank-pts { margin-left:auto; color:#8a8f99; font-size:12.5px; }
  .feed-item {
    display:flex; gap:10px; padding:10px 0; border-bottom:1px solid #f0f0f0;
    font-size:13px; color:#3a3d44;
  }
  .feed-item:last-child { border-bottom:none; }
  .feed-icon {
    width:34px; height:34px; border-radius:10px; background:#eef1f5;
    display:flex; align-items:center; justify-content:center; flex-shrink:0;
  }
  .cat-card {
    display:flex; align-items:center; gap:12px;
    background:#fff; border-radius:16px; padding:14px;
    margin-bottom:12px; box-shadow:0 2px 6px rgba(0,0,0,0.05);
  }
  .cat-icon {
    width:46px; height:46px; border-radius:12px; flex-shrink:0;
    display:flex; align-items:center; justify-content:center;
  }
  .cat-info { flex:1; }
  .cat-info .name { font-size:14.5px; font-weight:700; color:#111111; margin-bottom:6px; }
  .progress-bar { height:6px; background:#eef1f5; border-radius:6px; overflow:hidden; }
  .progress-fill { height:100%; background:#2f9e6e; border-radius:6px; }
  .cat-pct { font-size:11.5px; color:#8a8f99; margin-top:5px; }
  .q-card {
    background:#fff; border-radius:16px; padding:18px; margin-bottom:14px;
    box-shadow:0 2px 6px rgba(0,0,0,0.05);
  }
  .q-progress { font-size:12px; color:#8a8f99; margin-bottom:8px; font-weight:600; }
  .q-text { font-size:16px; font-weight:700; color:#111111; margin-bottom:16px; }
  .opt {
    display:block; width:100%; text-align:left; background:#f5f6f8;
    border:2px solid transparent; border-radius:12px; padding:12px 14px;
    margin-bottom:10px; font-size:13.5px; color:#2b2b2b; cursor:pointer;
  }
  .opt.correct { background:#e5f5ec; border-color:#2f9e6e; color:#1e6a49; font-weight:700; }
  .opt.wrong { background:#fbeaea; border-color:#d9534f; color:#9c2f2b; font-weight:700; }
  .pts-badge {
    display:inline-flex; align-items:center; gap:5px; background:#fff4de;
    color:#a06a06; font-size:12px; font-weight:700; padding:5px 10px; border-radius:20px;
  }
  .store-balance {
    display:flex; justify-content:space-between; align-items:center;
    background:#111111; color:#fff; border-radius:16px; padding:14px 18px; margin-bottom:14px;
  }
  .store-balance .n { font-size:22px; font-weight:700; color:#f2a71b; }
  .store-grid { display:grid; grid-template-columns:1fr 1fr; gap:12px; }
  .prize-card {
    background:#fff; border-radius:16px; overflow:hidden; box-shadow:0 2px 6px rgba(0,0,0,0.05);
  }
  .prize-img {
    height:80px; background:#eef1f5; display:flex; align-items:center; justify-content:center;
  }
  .prize-body { padding:10px; }
  .prize-name { font-size:12.5px; font-weight:700; color:#111111; margin-bottom:6px; min-height:32px; }
  .prize-cost { font-size:12px; color:#8a8f99; margin-bottom:8px; }
  .prize-btn {
    width:100%; background:#f2a71b; color:#111111; border:none; font-weight:700;
    font-size:12px; padding:8px; border-radius:10px; cursor:pointer;
  }
  .profile-header {
    display:flex; flex-direction:column; align-items:center; text-align:center; margin-bottom:18px;
  }
  .avatar-lg {
    width:78px; height:78px; border-radius:50%; background:#f2a71b; color:#111111;
    font-weight:700; font-size:28px; display:flex; align-items:center; justify-content:center;
    margin-bottom:10px;
  }
  .profile-header .name { font-size:17px; font-weight:700; color:#111111; }
  .profile-header .rank { font-size:13px; color:#8a8f99; margin-top:2px; }
  .stat-grid { display:grid; grid-template-columns:1fr 1fr; gap:10px; margin-bottom:14px; }
  .stat-box { background:#fff; border-radius:14px; padding:12px; text-align:center; box-shadow:0 2px 6px rgba(0,0,0,0.05); }
  .stat-box .v { font-size:19px; font-weight:700; color:#111111; }
  .stat-box .l { font-size:11px; color:#8a8f99; margin-top:2px; }
  .medal-grid { display:grid; grid-template-columns:repeat(4,1fr); gap:10px; }
  .medal { text-align:center; }
  .medal-icon {
    width:48px; height:48px; border-radius:50%; margin:0 auto 6px;
    display:flex; align-items:center; justify-content:center; font-size:20px;
  }
  .medal.locked .medal-icon { background:#eef1f5; color:#c3c7cf; }
  .medal.unlocked .medal-icon { background:#fff4de; color:#f2a71b; }
  .medal .m-label { font-size:10.5px; color:#8a8f99; }
  .bottomnav {
    position:absolute; bottom:0; left:0; right:0; height:74px;
    background:#fff; border-top:1px solid #ececec;
    display:flex; align-items:center; justify-content:space-around;
    padding-bottom:10px;
  }
  .navitem {
    display:flex; flex-direction:column; align-items:center; gap:3px;
    color:#9aa1ab; cursor:pointer; font-size:10.5px; font-weight:600;
    background:none; border:none;
  }
  .navitem.active { color:#111111; }
  .navitem svg { width:22px; height:22px; }
  .navitem.active svg { stroke:#111111; }
  .demo-role-bar {
    flex-shrink:0;
    display:flex;
    flex-wrap:wrap;
    align-items:center;
    gap:6px;
    padding:8px 16px;
    background:#fff4de;
    border-bottom:1px solid #f0e0bd;
    font-size:11px;
    color:#a06a06;
  }
  .demo-role-bar span { width:100%; margin-bottom:2px; }
  .role-toggle-btn {
    border:1px solid #e3c78a;
    background:#fff;
    color:#a06a06;
    font-size:11px;
    font-weight:600;
    padding:4px 10px;
    border-radius:20px;
    cursor:pointer;
  }
  .role-toggle-btn.active { background:#f2a71b; color:#111111; border-color:#f2a71b; }
  .wa-badge-role {
    display:inline-block;
    font-size:10.5px;
    font-weight:700;
    color:#a06a06;
    background:#fff4de;
    padding:3px 9px;
    border-radius:20px;
    margin-top:6px;
  }
  .wa-upload-btn {
    display:block;
    text-align:center;
    background:#f5f6f8;
    border:2px dashed #c3c7cf;
    color:#5a5e66;
    font-size:13px;
    font-weight:600;
    padding:16px;
    border-radius:12px;
    cursor:pointer;
  }
  .cat-card-wrap { margin-bottom:12px; }
  .cat-chevron { color:#c3c7cf; font-size:11px; margin-left:6px; }
  .lesson-list {
    background:#fff; border-radius:0 0 16px 16px; margin-top:-2px;
    box-shadow:0 2px 6px rgba(0,0,0,0.05); overflow:hidden;
  }
  .lesson-row {
    display:flex; align-items:center; gap:10px; padding:11px 16px;
    border-top:1px solid #f0f0f0; font-size:13px; color:#2b2b2b; cursor:pointer;
  }
  .lesson-check { font-size:15px; flex-shrink:0; }
  .lesson-title { flex:1; }
  .lesson-pts { font-size:11.5px; color:#8a8f99; }
  .prize-btn:disabled { cursor:default; }
  .toast {
    display:none; text-align:center; background:#111111; color:#fff;
    font-size:12.5px; font-weight:600; padding:9px 14px; border-radius:20px;
    margin-bottom:12px;
  }
  .toast.show { display:block; }
</style>
</head>
<body>

<div class="phone">
  <div class="statusbar"></div>
  <div class="appheader">
    <div class="appheader-top">
      <div class="logo-group">
        <img class="logo-catosa" src="catosa-logo-transparent.png" alt="Catosa">
        <div class="logo-divider"></div>
        <img class="logo-international" src="international-logo-transparent.png" alt="International">
      </div>
      <div class="profile-chip">
        <div class="avatar-sm">JP</div>
        <div class="points-chip" id="header-points">1,240 pts</div>
      </div>
    </div>
    <div class="greeting">Hola de nuevo<b>Juan Pérez</b></div>
    <div class="streak">🔥 5 días seguidos aprendiendo</div>
  </div>

  <div class="demo-role-bar">
    <span>Vista previa · rol:</span>
    <button class="role-toggle-btn active" data-role="operativo" onclick="setRole('operativo')">Operativo</button>
    <button class="role-toggle-btn" data-role="vendedor" onclick="setRole('vendedor')">Vendedor/Asesor</button>
    <button class="role-toggle-btn" data-role="administrativo" onclick="setRole('administrativo')">Administrativo</button>
  </div>

  <div id="screen-inicio" class="screen active">
    <div class="challenge-card">
      <div class="tag">Reto del día</div>
      <h3>Frenos de aire: lo esencial</h3>
      <p>5 preguntas · 3 min · +50 pts</p>
      <button class="btn-primary" onclick="goTo('retos')">Jugar ahora</button>
    </div>

    <div class="card">
      <div class="card-title">🏆 Top de tu sucursal</div>
      <div class="rank-row"><div class="rank-num">1</div> Rosa Martínez <div class="rank-pts">2,100 pts</div></div>
      <div class="rank-row"><div class="rank-num">2</div> Luis Hernández <div class="rank-pts">1,860 pts</div></div>
      <div class="rank-row me"><div class="rank-num">3</div> Tú (Juan Pérez) <div class="rank-pts" id="rank-me-pts">1,240 pts</div></div>
    </div>

    <div class="card">
      <div class="card-title">📣 Avisos</div>
      <div class="feed-item"><div class="feed-icon">🛠️</div> Nuevo módulo disponible: "Mantenimiento preventivo de suspensión".</div>
      <div class="feed-item"><div class="feed-icon">🎁</div> Llegaron nuevos souvenirs a la tienda de premios.</div>
      <div class="feed-item"><div class="feed-icon">🏁</div> Torneo entre sucursales inicia el próximo lunes.</div>
    </div>
  </div>

  <div id="screen-aprender" class="screen">
    <div class="card-title" style="padding:0 2px 4px;">Categorías</div>
    <div id="toast-aprender" class="toast"></div>
    <div id="learn-container"></div>
  </div>

  <div id="screen-retos" class="screen">
    <div id="trivia-container"></div>
    <div class="card">
      <div class="card-title">Próximos retos</div>
      <div class="feed-item"><div class="feed-icon">📅</div> Reto semanal: "Rutas y logística" — abre el viernes.</div>
      <div class="feed-item"><div class="feed-icon">🏁</div> Torneo entre sucursales — inicia el lunes.</div>
    </div>

    <div class="card role-vendedor-only" id="wa-mission-card" style="display:none;">
      <div class="card-title">📱 Evidencia de historias WhatsApp</div>
      <p style="font-size:12.5px; color:#5a5e66; margin-bottom:4px; line-height:1.4;">Misión exclusiva para vendedores/asesores. Sube la captura de tu historia de WhatsApp con las promociones y ofertas del día.</p>
      <div class="wa-badge-role">Solo vendedores/asesores</div>

      <div id="wa-pending" style="margin-top:12px;">
        <input type="file" id="wa-file" accept="image/*" style="display:none;" onchange="handleWaUpload(this)">
        <label for="wa-file" class="wa-upload-btn">📤 Elegir captura de mi historia</label>
        <div id="wa-preview" style="display:none; margin-top:10px;">
          <img id="wa-preview-img" style="width:100%; border-radius:10px; max-height:160px; object-fit:cover; display:block;">
          <button class="btn-primary" style="margin-top:10px; background:#111111; color:#fff;" onclick="confirmWaUpload()">Registrar evidencia</button>
        </div>
      </div>
      <div id="wa-done" style="display:none; margin-top:12px; text-align:center;">
        <span class="pts-badge">✓ Registrado hoy · +15 pts</span>
      </div>

      <div style="margin-top:14px; border-top:1px solid #f0f0f0; padding-top:10px;">
        <div style="font-size:12px; font-weight:700; color:#111111; margin-bottom:6px;">Historial reciente</div>
        <div class="feed-item"><div class="feed-icon">✅</div> 9 jul — Evidencia registrada · +15 pts</div>
        <div class="feed-item"><div class="feed-icon">✅</div> 8 jul — Evidencia registrada · +15 pts</div>
        <div class="feed-item"><div class="feed-icon">⚠️</div> 7 jul — Registrada fuera de tiempo · +5 pts</div>
      </div>
    </div>
  </div>

  <div id="screen-tienda" class="screen">
    <div class="store-balance">
      <div>Tus puntos</div>
      <div class="n" id="store-points">1,240</div>
    </div>
    <div id="toast-tienda" class="toast"></div>
    <div class="store-grid" id="store-grid"></div>
  </div>

  <div id="screen-perfil" class="screen">
    <div class="profile-header">
      <div class="avatar-lg">JP</div>
      <div class="name">Juan Pérez</div>
      <div class="rank">Rango: Operador · Mantenimiento</div>
    </div>
    <div class="stat-grid">
      <div class="stat-box"><div class="v" id="profile-points">1,240</div><div class="l">Puntos totales</div></div>
      <div class="stat-box"><div class="v">5</div><div class="l">Días seguidos</div></div>
    </div>
    <div class="card">
      <div class="card-title">Progreso a "Piloto Experto"</div>
      <div class="progress-bar"><div class="progress-fill" id="profile-progress-fill" style="width:62%;"></div></div>
      <div class="cat-pct" id="profile-progress-label">1,240 / 2,000 pts</div>
    </div>
    <div class="card">
      <div class="card-title">Medallas</div>
      <div class="medal-grid">
        <div class="medal unlocked"><div class="medal-icon">🛡️</div><div class="m-label">Seguridad</div></div>
        <div class="medal unlocked"><div class="medal-icon">🔥</div><div class="m-label">5 días</div></div>
        <div class="medal locked"><div class="medal-icon">🚛</div><div class="m-label">Experto camiones</div></div>
        <div class="medal locked"><div class="medal-icon">🥇</div><div class="m-label">Top 1 sucursal</div></div>
      </div>
    </div>
  </div>

  <div class="bottomnav">
    <button class="navitem active" data-screen="inicio" onclick="goTo('inicio')">
      <svg viewBox="0 0 24 24" fill="none" stroke="#9aa1ab" stroke-width="2"><path d="M3 12l9-9 9 9M5 10v10h14V10"/></svg>
      Inicio
    </button>
    <button class="navitem" data-screen="aprender" onclick="goTo('aprender')">
      <svg viewBox="0 0 24 24" fill="none" stroke="#9aa1ab" stroke-width="2"><path d="M4 5c3-2 6-2 8 0v14c-2-2-5-2-8 0V5zM20 5c-3-2-6-2-8 0v14c2-2 5-2 8 0V5z"/></svg>
      Aprender
    </button>
    <button class="navitem" data-screen="retos" onclick="goTo('retos')">
      <svg viewBox="0 0 24 24" fill="none" stroke="#9aa1ab" stroke-width="2"><path d="M8 21h8M12 17v4M6 3h12v4a6 6 0 01-12 0V3zM4 5H2v2a4 4 0 004 4M20 5h2v2a4 4 0 01-4 4"/></svg>
      Retos
    </button>
    <button class="navitem" data-screen="tienda" onclick="goTo('tienda')">
      <svg viewBox="0 0 24 24" fill="none" stroke="#9aa1ab" stroke-width="2"><path d="M3 9l1-5h16l1 5M4 9h16v11H4V9zM9 13a3 3 0 006 0"/></svg>
      Tienda
    </button>
    <button class="navitem" data-screen="perfil" onclick="goTo('perfil')">
      <svg viewBox="0 0 24 24" fill="none" stroke="#9aa1ab" stroke-width="2"><circle cx="12" cy="8" r="4"/><path d="M4 21c0-4 4-6 8-6s8 2 8 6"/></svg>
      Perfil
    </button>
  </div>
</div>

<script>
function goTo(name) {
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
  document.getElementById('screen-' + name).classList.add('active');
  document.querySelectorAll('.navitem').forEach(n => {
    n.classList.toggle('active', n.dataset.screen === name);
  });
}

function setRole(role) {
  document.querySelectorAll('.role-toggle-btn').forEach(b => {
    b.classList.toggle('active', b.dataset.role === role);
  });
  document.querySelectorAll('.role-vendedor-only').forEach(el => {
    el.style.display = (role === 'vendedor') ? 'block' : 'none';
  });
}

function handleWaUpload(input) {
  if (!input.files || !input.files[0]) return;
  var reader = new FileReader();
  reader.onload = function(e) {
    document.getElementById('wa-preview-img').src = e.target.result;
    document.getElementById('wa-preview').style.display = 'block';
  };
  reader.readAsDataURL(input.files[0]);
}

function confirmWaUpload() {
  document.getElementById('wa-pending').style.display = 'none';
  document.getElementById('wa-done').style.display = 'block';
}

var userPoints = 1240;
var nextLevelTarget = 2000;

function formatPts(n) { return n.toLocaleString('es-MX'); }

function updatePointsDisplay() {
  document.getElementById('header-points').textContent = formatPts(userPoints) + ' pts';
  document.getElementById('rank-me-pts').textContent = formatPts(userPoints) + ' pts';
  document.getElementById('profile-points').textContent = formatPts(userPoints);
  document.getElementById('store-points').textContent = formatPts(userPoints);
  var pct = Math.min(100, Math.round((userPoints / nextLevelTarget) * 100));
  document.getElementById('profile-progress-fill').style.width = pct + '%';
  document.getElementById('profile-progress-label').textContent = formatPts(userPoints) + ' / ' + formatPts(nextLevelTarget) + ' pts';
  renderPrizes();
}

function showToast(id, text) {
  var el = document.getElementById(id);
  el.textContent = text;
  el.classList.add('show');
  setTimeout(function() { el.classList.remove('show'); }, 2200);
}

var triviaQuestions = [
  {pregunta:'¿Cada cuánto debe revisarse la presión de los frenos de aire?', opciones:['Antes de cada viaje', 'Una vez al mes', 'Solo si hay ruido extraño', 'Nunca, se ajusta solo'], correcta:0, puntos:10},
  {pregunta:'¿Qué debes hacer si un tambor de freno está sobrecalentado?', opciones:['Seguir manejando normal', 'Detenerte y dejar enfriar', 'Echarle agua fría de inmediato', 'Ignorarlo si no truena'], correcta:1, puntos:10},
  {pregunta:'¿Cuál es la función principal del secador de aire?', opciones:['Enfriar el motor', 'Quitar humedad del sistema de frenos', 'Aumentar la potencia', 'Limpiar el parabrisas'], correcta:1, puntos:10},
  {pregunta:'¿Qué indica una luz de advertencia de baja presión de aire?', opciones:['Nivel de combustible bajo', 'Fuga o falla en el sistema de frenos', 'Necesitas más aire en las llantas', 'Es solo decorativa'], correcta:1, puntos:10},
  {pregunta:'¿Cada cuándo se recomienda drenar los tanques de aire?', opciones:['Nunca', 'Cada semana o según el manual', 'Solo una vez al año', 'Cuando se descompone el camión'], correcta:1, puntos:10}
];
var triviaIndex = 0;
var triviaScore = 0;
var triviaAnswered = false;

function renderTrivia() {
  var container = document.getElementById('trivia-container');
  if (triviaIndex >= triviaQuestions.length) {
    container.innerHTML =
      '<div class="q-card" style="text-align:center;">' +
        '<div style="font-size:34px; margin-bottom:8px;">🎉</div>' +
        '<div class="q-text" style="margin-bottom:6px;">¡Reto completado!</div>' +
        '<div style="font-size:13px; color:#5a5e66; margin-bottom:14px;">Ganaste ' + triviaScore + ' pts en total</div>' +
        '<button class="btn-primary" style="background:#111111; color:#fff;" onclick="restartTrivia()">Volver a jugar</button>' +
      '</div>';
    return;
  }
  var q = triviaQuestions[triviaIndex];
  var optsHtml = q.opciones.map(function(opt, i) {
    return '<button class="opt" onclick="answerTrivia(' + i + ')">' + opt + '</button>';
  }).join('');
  container.innerHTML =
    '<div class="q-card">' +
      '<div class="q-progress">Pregunta ' + (triviaIndex + 1) + ' de ' + triviaQuestions.length + '</div>' +
      '<div class="q-text">' + q.pregunta + '</div>' +
      optsHtml +
      '<div id="trivia-feedback" style="text-align:center; margin-top:6px; min-height:26px;"></div>' +
    '</div>';
}

function answerTrivia(i) {
  if (triviaAnswered) return;
  triviaAnswered = true;
  var q = triviaQuestions[triviaIndex];
  var buttons = document.querySelectorAll('#trivia-container .opt');
  buttons.forEach(function(btn, idx) {
    btn.disabled = true;
    if (idx === q.correcta) btn.classList.add('correct');
    else if (idx === i) btn.classList.add('wrong');
  });
  var feedback = document.getElementById('trivia-feedback');
  if (i === q.correcta) {
    triviaScore += q.puntos;
    userPoints += q.puntos;
    feedback.innerHTML = '<span class="pts-badge">+' + q.puntos + ' pts ganados</span>';
  } else {
    feedback.innerHTML = '<span class="pts-badge" style="background:#fbeaea; color:#9c2f2b;">Correcta: ' + q.opciones[q.correcta] + '</span>';
  }
  updatePointsDisplay();
  setTimeout(function() {
    triviaIndex++;
    triviaAnswered = false;
    renderTrivia();
  }, 1400);
}

function restartTrivia() {
  triviaIndex = 0;
  triviaScore = 0;
  renderTrivia();
}

var learnCategories = [
  {
    nombre:'Seguridad', icon:'🦺', bg:'#fbeaea', barColor:'#2f9e6e',
    lecciones:[
      {titulo:'Uso correcto de EPP', puntos:10, completada:true},
      {titulo:'Procedimiento en caso de derrame', puntos:10, completada:true},
      {titulo:'Manejo defensivo básico', puntos:10, completada:true},
      {titulo:'Bloqueo y etiquetado (LOTO)', puntos:10, completada:false},
      {titulo:'Uso de extintores', puntos:10, completada:false}
    ]
  },
  {
    nombre:'Conocimiento de camiones', icon:'🚛', bg:'#eef1f5', barColor:'#4a4a4a',
    lecciones:[
      {titulo:'Sistema de frenos de aire', puntos:10, completada:true},
      {titulo:'Motor y transmisión básica', puntos:10, completada:true},
      {titulo:'Sistema eléctrico', puntos:10, completada:false},
      {titulo:'Suspensión y ejes', puntos:10, completada:false}
    ]
  },
  {
    nombre:'Procesos de taller', icon:'⚙️', bg:'#faeeda', barColor:'#EF9F27',
    lecciones:[
      {titulo:'Orden de servicio paso a paso', puntos:10, completada:true},
      {titulo:'Solicitud de refacciones', puntos:10, completada:false},
      {titulo:'Checklist de entrega', puntos:10, completada:false}
    ]
  }
];
var openCategory = null;

function renderLearn() {
  var container = document.getElementById('learn-container');
  container.innerHTML = learnCategories.map(function(cat, ci) {
    var total = cat.lecciones.length;
    var done = cat.lecciones.filter(function(l) { return l.completada; }).length;
    var pct = Math.round((done / total) * 100);
    var isOpen = openCategory === ci;
    var lessonsHtml = cat.lecciones.map(function(l, li) {
      return '<div class="lesson-row" onclick="toggleLesson(event,' + ci + ',' + li + ')">' +
        '<span class="lesson-check">' + (l.completada ? '✅' : '⬜') + '</span>' +
        '<span class="lesson-title">' + l.titulo + '</span>' +
        '<span class="lesson-pts">+' + l.puntos + ' pts</span>' +
      '</div>';
    }).join('');
    return '<div class="cat-card-wrap">' +
      '<div class="cat-card" onclick="toggleCategory(' + ci + ')" style="cursor:pointer; margin-bottom:' + (isOpen ? '0' : '12px') + ';">' +
        '<div class="cat-icon" style="background:' + cat.bg + ';">' + cat.icon + '</div>' +
        '<div class="cat-info">' +
          '<div class="name">' + cat.nombre + '</div>' +
          '<div class="progress-bar"><div class="progress-fill" style="width:' + pct + '%; background:' + cat.barColor + ';"></div></div>' +
          '<div class="cat-pct">' + done + ' de ' + total + ' lecciones completadas</div>' +
        '</div>' +
        '<span class="cat-chevron">' + (isOpen ? '▲' : '▼') + '</span>' +
      '</div>' +
      (isOpen ? '<div class="lesson-list">' + lessonsHtml + '</div>' : '') +
    '</div>';
  }).join('');
}

function toggleCategory(ci) {
  openCategory = (openCategory === ci) ? null : ci;
  renderLearn();
}

function toggleLesson(evt, ci, li) {
  evt.stopPropagation();
  var lesson = learnCategories[ci].lecciones[li];
  if (!lesson.completada) {
    lesson.completada = true;
    userPoints += lesson.puntos;
    showToast('toast-aprender', '✓ Lección completada · +' + lesson.puntos + ' pts');
  } else {
    lesson.completada = false;
    userPoints -= lesson.puntos;
  }
  updatePointsDisplay();
  renderLearn();
}

var prizesData = [
  {icono:'🧢', nombre:'Gorra Catosa', costo:500},
  {icono:'🥤', nombre:'Termo Catosa', costo:800},
  {icono:'🎒', nombre:'Mochila', costo:1500},
  {icono:'🎧', nombre:'Audífonos', costo:1200}
];

function renderPrizes() {
  var grid = document.getElementById('store-grid');
  if (!grid) return;
  grid.innerHTML = prizesData.map(function(p, i) {
    var afford = userPoints >= p.costo;
    var btnHtml = afford
      ? '<button class="prize-btn" onclick="redeemPrize(' + i + ')">Canjear</button>'
      : '<button class="prize-btn" style="background:#eef1f5; color:#9aa1ab;" disabled>Te faltan ' + formatPts(p.costo - userPoints) + '</button>';
    return '<div class="prize-card">' +
      '<div class="prize-img">' + p.icono + '</div>' +
      '<div class="prize-body">' +
        '<div class="prize-name">' + p.nombre + '</div>' +
        '<div class="prize-cost">' + formatPts(p.costo) + ' pts</div>' +
        btnHtml +
      '</div>' +
    '</div>';
  }).join('');
}

function redeemPrize(i) {
  var p = prizesData[i];
  if (userPoints < p.costo) return;
  userPoints -= p.costo;
  updatePointsDisplay();
  showToast('toast-tienda', '✓ ' + p.nombre + ' canjeado');
}

renderTrivia();
renderLearn();
renderPrizes();
</script>

</body>
</html>
